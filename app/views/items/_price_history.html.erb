<div id="price_history">
<% unless @price_history.empty? %>
  <div class="panel clearfix">
    <h3>Price History</h3>
    <ul>
      <% @price_history.each do |change| %>
        <%= number_to_currency change.price %>&nbsp;&nbsp;&nbsp;(<%= change.start_date.strftime('%m/%d/%y') %>)<br />
      <% end %>
    </ul>

    <%= link_to(new_item_price_path(this_item_id: @item.id), class: 'button tiny radius', id: 'new_item_price', remote: true) do %>
        Change Price
    <% end %>
  </div>
<% end %>
</div>