<%= simple_form_for @purchase, html: { class: 'form-horizontal', multipart: true }, remote: true do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Purchase</legend>

    <%#= f.input :item_id, collection: Item.active.alphabetical.to_a, include_blank: true, input_html: {class: "avg"} %>
    <%= f.hidden_field :item_id, value: @item_id %>
    <%= f.input :quantity, input_html: {class: "avg"} %>


    <div class="form-actions">
      <%= f.submit nil, :class => 'button radius' %>
      <%= link_to 'Cancel', purchases_path, :class => 'button alert radius' %>
    </div>
  </fieldset>
<% end %>