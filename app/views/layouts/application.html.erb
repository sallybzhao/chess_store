<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Chess Warehouse :: <%= controller.controller_name.capitalize %></title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div id="title_area">
      <%= link_to image_tag('chess_store_title.png'), home_path %>
    </div>
    <%= render :partial => "partials/nav" %>
    
    <div class="row">
      <div id="login">
        <!-- content once logged in goes here -->
        <% if logged_in? %>
          <%= link_to 'Logout', logout_path %>
          <br>[<%= current_user.proper_name %>:<%= current_user.role %>]
        <% else %>
          <%= link_to 'Login', login_path %>
        <% end %>
      </div>
      <div class="small-12 columns" id="main_content">
        <%= render :partial => "partials/flash" %>
        <%= yield %>
      </div>
    </div>
    <footer>
      <div class="small-12 columns" id="footer">
        <%= render partial: 'partials/footer' %>
      </div>
    </footer> 
    <%= javascript_include_tag "application" %>
  </body>
</html>
